{
  "name": "@pinefile/migrate",
  "version": "1.0.0",
  "description": "Migrate from other task runners",
  "main": "index.js",
  "bin": {
    "pine-migrate": "bin/migrate"
  },
  "scripts": {
    "format": "prettier --write index.js lib/**/*.js",
    "lint": "semistandard index.js lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:test": "echo \"Error: no test specified\" && exit 1",
    "test:npm": "npm run test"
  },
  "author": "Fredrik Forsmo <fredrik.forsmo@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@pinefile/pine": "^1.3.0",
    "stringify-object": "3.0.0"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "semistandard": "^16.0.1"
  }
}
